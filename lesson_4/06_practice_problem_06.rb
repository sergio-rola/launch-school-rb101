['ant', 'bear', 'caterpillar'].pop.size

# => 11
# #pop removes the last element from self and returns it. This means that "caterpillar" is returned from the method. Then use the String#size method that returns the character length of str.
